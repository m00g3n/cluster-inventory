package provisioning

import (
	"github.com/stretchr/testify/require"
	"testing"
)

func TestDynamicKubeconfig(t *testing.T) {
	t.Run("TBD: should return error when failed to get dynamic kubeconfig", func(t *testing.T) {
		// given

		// when

		// then
		require.True(t, true)
	})

	t.Run("TBD: should return valid dynamic kubeconfig", func(t *testing.T) {
		// given

		// when

		// then
		require.True(t, true)
	})

	t.Run("TBD: should force rotation of dynamic kubeconfig", func(t *testing.T) {
		// given

		// when

		// then
		require.True(t, true)
	})
}
